using System;
using OpenUI.Mathematics;

public static class Program {
  public static void Main(string[] args) {
    var pos = new Vector2(10 * sizeof(int), 5 * sizeof(int));
    Console.WriteLine(pos);
  }
}
